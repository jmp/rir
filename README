rir
===

This is a very simple IRC bot. You can control it by sending a PRIVMSG to
it, or addressing it by its name. For example:

	rir: join #eggs

There are no predefined commands.
All commands are sent raw to the server.
